# Android build artifacts (temporary)

`tolaria-0.1.0-arm64-release.apk` — arm64-v8a release build, signed with the
debug keystore for testing. Committed only so it can be downloaded from
GitHub; **drop this directory before merging to main** (binaries do not
belong in source history).
